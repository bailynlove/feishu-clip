# 飞书网页剪藏

这个上下文描述用户把浏览器中的网页内容一次性保存为飞书知识库文档时涉及的核心概念。

## Language

**网页剪藏（Web Clip）**:
来源页面在某一时刻的可读内容与来源信息快照，保存后成为一篇新的飞书文档。
_Avoid_: 网页导出、书签、同步副本

**来源页面（Source Page）**:
用户当前选择进行剪藏的浏览器页面。
_Avoid_: 原文档、源文件

**保存目标（Save Destination）**:
用户选定、用于接收新剪藏文档的位置：一个现有 Wiki 父节点，或一个知识空间的根层。
_Avoid_: 导出路径、同步目录

**剪藏尝试（Clip Attempt）**:
用户发起的一次保存请求；即使来源页面相同，每次尝试也请求创建一篇新文档。
_Avoid_: 更新、同步、覆盖

**部分成功（Partial Success）**:
剪藏文档及正文已经创建，但一张或多张图片未能嵌入；失败位置保留原图链接，并向用户展示警告。
_Avoid_: 导出失败、静默跳过

**图片候选（Image Candidate）**:
从来源页面识别出的一个图片位置；通过安全与资源校验后可成为嵌入图片，否则成为原图链接或安全占位文字。
_Avoid_: 图片文件、上传素材

**配对（Pairing）**:
用户授权一个具体浏览器扩展实例连接本机 Bridge 的关系；该关系同时绑定扩展 Origin 与专用访问凭据。
_Avoid_: 登录、飞书授权、仅校验扩展 ID

**配对码（Pairing Code）**:
用户为建立或替换配对而临时生成的一次性授权信息；它不等同于配对成功后的长期访问凭据。
_Avoid_: 访问令牌、飞书验证码

**预设（Preset）**:
一套命名的剪藏参数组合：保存目标、包含图片、标题模板、追加正文模板、默认保存动作与 URL 触发规则；预设列表中有一套为默认预设，弹窗内的改动不回写预设。
_Avoid_: 模板方案、配置档案

**模板（Template）**:
预设中支持 `{{}}` 变量语法的标题或追加正文文本；`{{content}}` 是剪藏正文在正文模板中的显式占位符。
_Avoid_: 格式串、占位文本

**悬浮内容（Hover Content）**:
来源页面中默认隐藏、悬停才展示的内容（如代码行内的灯泡注解面板）。剪藏时在触发位置留 `悬浮内容{i}` 标记，文字内容作为注释段落 `悬浮内容{i}: 内容` 排到所在块之后（列表项内的提升到整个列表之后），面板内的图片按 `悬浮内容{i}` 标注并锚定在同一位置。含 iframe 的隐藏容器不作悬浮打标。
_Avoid_: 隐藏内容、提示框、工具提示

**iframe 占位符（iframe Placeholder）**:
正文内 iframe（如折叠起来的算法可视化面板）在剪藏正文中的 `[[FEISHU_CLIP_IFRAME:i]]` 独立段落标记；桥接侧据此转写为飞书 iframe 块（block_type 26），失败时降级为标题链接段落并警告。
_Avoid_: 嵌入帧、外部网页块
